#include "main.h"

/**
 * print_last_digit - function that prints last digit
 *
 * @j: function parameter
 *
 * Return: h
 */

int print_last_digit(int j)
{
	int h;

	h = j % 10;
	if (j < 0)
		h = -h;
	return (j);
}
