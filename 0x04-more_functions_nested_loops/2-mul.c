#include "main.h"

/**
 * mul - function that multiples two integers
 * @x: first number to multiply
 * @y: second number to be multiply
 * Return: result of multiplication between x parameter and y parameter
 */

int mul(int x, int y)
{
	return (x * y);
}
